'use strict';


//Obtain properties
//Pre load all ref
//If its ref find by its id or create new one
//Return partial populated(use mongoose-autopopulate)
//Cache created model
exports = module.exports = function(request, response, next) {
  // body...
  next();
};